---
summary: "CLI reference for `alphaclaw uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `alphaclaw uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
alphaclaw uninstall
alphaclaw uninstall --all --yes
alphaclaw uninstall --dry-run
```
